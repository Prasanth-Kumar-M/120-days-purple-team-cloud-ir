# Day 14-16 Commands

- **day14_cloudtrail_commands.sh** – CLI commands to explore CloudTrail fields.
- **day15_kibana_queries.sh** – Kibana queries for ConsoleLogin failures and GetObject events.
- **day16_awscli_events.sh** – CLI commands to trigger CloudTrail events (create/list/delete S3 buckets).

**Instructions:**  
Run the `.sh` files in a terminal with AWS CLI configured.
